<div class="container container-size container-border my-4 bg-white">
  <div class="profile text-center mt-4">
    <div class="row">
      <div class="col-6">
        <%= image_tag @user.image, class: "pics rounded-circle float-right mt-3" %>
      </div>
      <div class="col-6 mt-5">
        <h3 class="nm-text text-left"><%= @user.name %></h3>
        <i class="fas fa-map-marker-alt"></i>
      </div>
    </div>
  </div>
</div>

<div class="container my-4">
  <div class="row">
    <div class="col-md-3 bg-white container-border">
      <ul class="navbar-nav">
        <li class="nav-item active-link link mx-n3">
          <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item link mx-n3">
          <a class="nav-link" href="#">Link</a>
        </li>
        <li class="nav-item dropdown link mx-n3">
          <% if !is_admin? %>
            <%= link_to "Become an admin", admin_registration_path, class: "nav-link" %>
          <% elsif is_admin? %>
            <%= link_to "Add a restaurant category", new_restaurant_category_path, class: "nav-link" %>
            <%= link_to "Add a restaurant", new_restaurant_path, class: "nav-link" %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</div>