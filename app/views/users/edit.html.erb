<% @page_title = "Add Info" %>

<div class="container container-size container-border my-4 bg-white">
  <div class="profile text-center mt-4">
    <div class="row">
      <div class="col-6">
        <%= image_tag @user.image.url, class: "pics rounded-circle float-right mt-3" %>
      </div>
      <div class="col-6 mt-5">
        <h3 class="nm-text text-left"><%= @user.name %></h3>
        <i class="fas fa-map-marker-alt"></i>
      </div>
    </div>
  </div>
</div>

<div class="container container-size my-4">
  <div class="row">
    <div class="col-md-2">
      
    </div>


    <div class="col-md-6 ml-3">
      <div class="container-border bg-white p-3 mx-n3" id="changeContainer">
        <%= form_for @user do |f| %>

          <% if @user.errors.any? %>
            <% @user.errors.full_messages.each do |message| %>
              <p class="text-danger"><%= message %></p>
            <% end %>
          <% end %>

          <div id="name">
            <h5 class="font-weight-bold">Change Profile Name</h5>
            <div class="form-group">
              <%= f.label :name, "Change profile name" %>
              <%= f.text_field :name, class: "form-control" %>
            </div>
          </div>
          <div id="image">
            <h5 class="font-weight-bold">Change Profile pics</h5>
            <div class="form-group">
              <%= f.label :image, "Change profile image" %>
              <%= f.file_field :image, class: "form-control-file" %>
            </div>
          </div>
          <div id="phone">
            <h5 class="font-weight-bold">Change Phone Number</h5>
            <div class="form-group">
              <%= f.label :phone_number, "Change phone number" %>
              <%= f.text_field :phone_number, placeholder: "Enter valid Phone number", class: "form-control" %>
            </div>
          </div>

          <div id="password">
            <h5 class="font-weight-bold">Change password</h5>
            <div class="form-group">
              <%= f.label :password %>
              <%= f.password_field :password, placeholder: "For change password", class: "form-control" %>
            </div>
            <div class="form-group">
              <%= f.label :password_confirmation %>
              <%= f.password_field :password_confirmation, placeholder: "password confirmation for change password", class: "form-control" %>
            </div>
          </div>

          <div id="btn">
            <%= f.submit "Save changes", class: "btn btn-primary" %>
            <%= link_to "Back", user_path(@user), class: "btn btn-secondary" %>
          </div>
        <% end %>
      </div>
    </div>

    <div class="col-md-3 ml-auto">
      <div class="container-border bg-white px-4 py-3">
        <h5 class="font-weight-bold">How it works</h5>
        <ul>
          <li class="small my-2">If you want to change only <strong class="text-danger">profile name</strong>, then fill only <strong class="text-danger">change profile name</strong> field.</li>

          <li class="small my-2">If you want to change / add only <strong class="text-danger">profile pics</strong>, then fill only <strong class="text-danger">change profile image</strong> field.</li>

          <li class="small my-2">If you want to change / add only <strong class="text-danger">phone number</strong>, then fill only <strong class="text-danger">change Phone number</strong> field.</li>

          <li class="small my-2">If you want to change only <strong class="text-danger">password</strong> then fill only <strong class="text-danger">change password</strong> fields and that will work only sign up using <strong class="text-danger">Facebook or Google</strong>.</li>

          <li class="small my-2">If you want to cahnge <strong class="text-primary">multiple fields</strong>, then fill <strong class="text-danger">multiple fields</strong>.</li>

          <li class="small my-2">At last press <strong class="text-primary">Save changes</strong> button to save changes.</li>
        </ul>
      </div>
    </div>
  </div>
</div>
